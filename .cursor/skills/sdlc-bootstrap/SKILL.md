# Cursor Skill: sdlc-bootstrap

Source: `.github/skills/sdlc-bootstrap/SKILL.md`

Workflow outcome:

- Bootstrap Cursor compatibility by creating or refreshing:
  - `.cursor/rules/*.mdc`
  - `.cursor/agents/*.agent.md`
  - `.cursor/skills/*.md`
  - `AGENTS.md`

Constraints:

- Keep `.github/sdlc-config.json` as source of truth.
- Do not overwrite `.github` SDLC source assets unless explicitly asked.
